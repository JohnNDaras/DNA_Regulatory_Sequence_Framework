name: genomic-sequence-framework
channels: [conda-forge, pytorch]
dependencies:
  - python=3.11
  - numpy
  - pandas
  - pyyaml
  - scikit-learn
  - matplotlib
  - pytorch
  - pip
  - pip:
      - logomaker
      - tqdm
      - pytest
